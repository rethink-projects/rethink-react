import initialState from 'esfera/store/initialState';
import {} from '../types';

const <%= props.name %>Reducer = (state = initialState.auth, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default <%= props.name %>Reducer;
